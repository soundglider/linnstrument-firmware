/**************************** ls_chord_melody: (deferred) *****************************************
The melody zone (right-half top 6 rows) is intentionally delegated to stock LinnStrument
behavior — its note resolution and LED paint follow the user's per-split settings
(channel, note-lights, scale, etc.), so this file is currently empty.

If a chord-engine-aware melody mode is ever wanted (the original Phase 7 / Phase 10
spec in docs/ROADMAP.md), this is where melody_note_for_cell + scale_degrees would live.
**************************************************************************************************/
